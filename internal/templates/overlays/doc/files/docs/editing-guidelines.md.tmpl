# Editing Guidelines

Editorial guidance for any agent or contributor working in this repo.
These are durable formatting and structural practices, not workflow or process rules.
For workflow, see `editing-cycle.md`. For release, see `release.md`.

## Cross-Reference Integrity

- When a doc references another file, section, or heading, verify the target exists before publishing
- When renaming or moving a file, grep the repo for all references and update them in the same pass
- When deleting a section or heading, check for inbound links from other docs

## Link Integrity

- Internal links use repo-relative paths
- External links include the full URL
- Verify links are not stale before publishing; flag broken links as findings during review

## Structural Conventions

- Use consistent heading hierarchy within each document
- Do not skip heading levels (e.g., jump from `##` to `####`)
- Keep one topic per file; split when a file covers unrelated concerns
- Use flat `##` sections with inline bullets; avoid deep nesting unless structure genuinely requires it

## Formatting Rules

- Prefer plain text and simple bullets over tables or rich formatting unless content clearly benefits
- Use code fences for file paths, commands, and configuration values
- Keep lines readable — no hard wraps at arbitrary column widths, but avoid single-paragraph walls of text
- Use consistent terminology throughout the repo; define terms in one place and reference that definition

## Documentation Alignment

- Docs ship with the content change that introduces the behavior
- If a doc references a file path or section heading, verify it still exists before publishing
- When a decision changes, update all affected docs in the same pass — no half-migrated states
