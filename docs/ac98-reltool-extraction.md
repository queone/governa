# AC98 reltool extraction

Skeleton stub. Extracts `internal/reltool` to `github.com/queone/governa-reltool` under the policy in [`docs/library-policy.md`](library-policy.md). Convention-coupling test must be re-applied at draft time — `reltool` parses git tags and does not appear to encode governance conventions, but verify before drafting (CHANGELOG awareness, governance-file dependencies). First-Consumer Self-Test runs in `cmd/rel/main.go` (already imports `internal/reltool`).

## Summary

Stub awaiting full draft. When activated, the AC will be drafted in full per `docs/ac-template.md`, critiqued, and authorized before implementation. See AC96 for policy context.

## Status

`PENDING` — stub registered as IE8 in `plan.md`. Awaiting Director scope-up and full draft.
