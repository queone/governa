# AC97 color extraction

Skeleton stub. Extracts `internal/color` to a separate-repo Go library at `github.com/queone/governa-color` under the policy in [`docs/library-policy.md`](library-policy.md). First extraction in the library-extraction track set by AC96; sets the precedent for the family. Library is leaf-clean (zero governance coupling), so the convention-coupling test is expected to admit a clean extraction without a template-side adapter. First-Consumer Self-Test runs in `cmd/governa/main.go` (already imports `internal/color`).

## Summary

Stub awaiting full draft. When activated, the AC will be drafted in full per `docs/ac-template.md`, critiqued, and authorized before implementation. See AC96 for policy context, the library-extraction track, and the per-extraction sequence.

## Status

`PENDING` — stub registered as IE7 in `plan.md`. Awaiting Director scope-up and full draft.
